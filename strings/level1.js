let userName = "ram";
let age = "28";
let address = "swoyambhu";

// get the length of the name string
let length = userName.length;
console.log(length);

// City to uppercase
let uppercase = address.toUpperCase();
console.log(uppercase);
